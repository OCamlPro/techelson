parameter nat;
storage (pair (pair nat int) (pair int (pair nat (pair int nat))));
code { DUP ;
       DIP { CDR @storage_slash_1 } ;
       CAR @parameter_slash_2 ;
       PUSH int 42 ;
       DROP ;
       RANDOM @x ;
       DUUP @parameter ;
       PAIR ;
       SWAPP @p nat int ;
       MANY ;
       DIIP { PAIR } ;
       DIP { PAIR } ;
       PAIR ;
       DUUP @p ;
       CDR ;
       DUUUP @p ;
       CAR ;
       SWAP int nat ;
       DIIIP { DROP ; DROP ; DROP } ;
       PAIR ;
       PAIR ;
       NIL operation ;
       PAIR };
