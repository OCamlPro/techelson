parameter (contract :UnitContract unit);
storage (pair (map int mutez) (contract :UnitContract unit));
code { DUP ;
       DIP { CDR @storage_slash_1 } ;
       CAR @parameter_slash_2 ;
       DUUP @storage ;
       NIL operation ;
       PUSH bool True ;
       LOOP { RENAME @acc_slash_4 ;
              DUUUUP @storage ;
              CDR ;
              PUSH mutez 1000000 ;
              UNIT ;
              TRANSFER_TOKENS @op ;
              SWAP ;
              SWAP ;
              CONS ;
              PUSH bool False ;
              PAIR ;
              DUP ;
              CAR ;
              DIP { CDR } } ;
       DIIP { DROP ; DROP } ;
       RENAME @acc ;
       PAIR };
