parameter timestamp;
storage
  (pair string
        (pair timestamp
              (pair (pair mutez mutez)
                    (pair (contract :UnitContract unit)
                          (pair (contract :UnitContract unit) (contract :UnitContract unit))))));
code { CDR @storage_slash_1 ;
       DUP ;
       DUP ;
       CDDDDDR @b ;
       DUUP ;
       CDDDDAR @a ;
       PAIR ;
       DUUUP ;
       CDDDAR @x ;
       PAIR ;
       DUUUP ;
       DIIIP { DROP } ;
       CDDAR @pn ;
       PAIR ;
       DUUP ;
       CDAR @t ;
       PAIR ;
       SWAP ;
       CAR @s ;
       PAIR @storage ;
       NIL operation ;
       PAIR };
